# Compose overrides applied automatically when you run `docker compose up`.
# Keep this file minimal and production-like; no hot-reload/dev server overrides.

services:

  db:
    ports:
      - "${DB_PORT:-5432}:5432"
